<h1><%= @artist.id ? "Edit Artist" : "Create Artist" %></h1>
<br />
<%= form_with(model: [@board, @artist], local: true, class: "ui form") do |f| %>
<%= f.label :name %>
<%= f.text_field :name, required: true, autofocus: true %>
<br/>
<%= f.label :genre %>
<%= f.text_field :genre, required: true, autofocus: true %>
<br/>
<br/>
<%# I don't understand why the nil is included with the submit button %>
<%= f.submit nil, class: "ui button" %>
<% end %>