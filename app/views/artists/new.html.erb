<h1>Add an Artist</h1>
<%# The code below is to render the form, and then pass the data into the corresponding array %>
<%= render "form", board: @board, artist: @artist %>
<%= link_to "Back", board_artists_path(@board) %>
<br/>

